class User {
    constructor(id, name, createdDate) {
        this.id = id;
        this.name = name;
        this.createdDate = createdDate;
    }
}

//method for User entity data access

module.exports = {User}